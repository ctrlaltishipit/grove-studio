# Grove — CI. Two jobs: the web app (typecheck, build, independence audit,
# unit tests) and the stdlib-only Python functions (pytest).
# The audit runs AFTER the build on purpose: rule 7 inspects dist/assets.
name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  web:
    name: Web (Node 20)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      - run: npm run audit
      # A gate nobody has tried to defeat is decoration.
      - run: npm run audit:selftest
      - run: npx vitest run --passWithNoTests

  python:
    name: API (Python 3.12)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements-dev.txt
      - run: python -m pytest tests/python -q
